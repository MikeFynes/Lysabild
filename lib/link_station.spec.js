const fs = require('fs')
const request = require('request')

const expect = require('chai').expect

const webpage = require('./link_station')

describe('The devices power', function () {
    it('Finds the link station that provides most power to device', function* () {

    })
}
